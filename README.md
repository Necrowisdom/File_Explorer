# File_Explorer
A Command Prompt utility that scans all local and external drives (including USBs) for a specific filename. It identifies duplicates, sorts them by date, and returns the directory path of the most recent file.
